import React from 'react';

const TestPage = () => {
  // React.useEffect(() => {
  //   console.log(process);
  // })
  return (
    <div>
      <h1>{JSON.stringify(process)}</h1>
    </div>
  );
};

export default TestPage;
